import React from 'react';
import { withRouter } from 'react-router-dom';

const MainINFO = props => {
  console.log('MainINFO', props);

  return (
    <main>
      <h5>MainINFO</h5>
    </main>
  );
};

export default MainINFO;
// export default withRouter(MainINFO);
